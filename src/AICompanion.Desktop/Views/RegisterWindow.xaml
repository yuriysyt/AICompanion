<Window x:Class="AICompanion.Desktop.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AI Companion - Register"
        Height="580"
        Width="420"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Border CornerRadius="15"
            Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource PrimaryBrush}"
            BorderThickness="2">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20"
                              ShadowDepth="5"
                              Opacity="0.3"
                              Color="{StaticResource PrimaryColor}"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    Background="{StaticResource PrimaryGradientBrush}"
                    CornerRadius="13,13,0,0"
                    Padding="20,15"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="Create Account"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="White"
                               VerticalAlignment="Center"/>
                    <Button Content="X"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Width="30" Height="30"
                            Background="Transparent"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="14"
                            Click="CloseButton_Click"
                            Cursor="Hand"/>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="30,20,30,30">

                    <TextBlock Text="Sign up to get started"
                               FontSize="13"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,20"/>

                    <!-- Username -->
                    <TextBlock Text="Username"
                               FontWeight="SemiBold"
                               Margin="0,0,0,6"/>
                    <TextBox x:Name="UsernameBox"
                             Padding="12,10"
                             FontSize="14"
                             Margin="0,0,0,12"/>

                    <!-- Email -->
                    <TextBlock Text="Email (optional)"
                               FontWeight="SemiBold"
                               Margin="0,0,0,6"/>
                    <TextBox x:Name="EmailBox"
                             Padding="12,10"
                             FontSize="14"
                             Margin="0,0,0,12"/>

                    <!-- Password -->
                    <TextBlock Text="Password"
                               FontWeight="SemiBold"
                               Margin="0,0,0,4"/>
                    <TextBlock Text="Min. 8 characters, 1 uppercase letter, 1 digit"
                               FontSize="11"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,0,0,6"/>
                    <PasswordBox x:Name="PasswordBox"
                                 Padding="12,10"
                                 FontSize="14"
                                 Margin="0,0,0,12"/>

                    <!-- Confirm Password -->
                    <TextBlock Text="Confirm Password"
                               FontWeight="SemiBold"
                               Margin="0,0,0,6"/>
                    <PasswordBox x:Name="ConfirmPasswordBox"
                                 Padding="12,10"
                                 FontSize="14"
                                 Margin="0,0,0,12"/>

                    <!-- PIN -->
                    <TextBlock Text="PIN Code"
                               FontWeight="SemiBold"
                               Margin="0,0,0,4"/>
                    <TextBlock Text="4 to 6 digits — used to confirm sensitive actions"
                               FontSize="11"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,0,0,6"/>
                    <PasswordBox x:Name="PinBox"
                                 MaxLength="6"
                                 Padding="12,10"
                                 FontSize="14"
                                 Margin="0,0,0,20"/>

                    <!-- Error Message -->
                    <TextBlock x:Name="ErrorMessage"
                               Foreground="{StaticResource ErrorBrush}"
                               FontSize="12"
                               TextWrapping="Wrap"
                               Margin="0,0,0,10"
                               Visibility="Collapsed"/>

                    <!-- Register Button -->
                    <Button Content="Create Account"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="Register_Click"
                            Margin="0,0,0,15"/>

                    <!-- Back to Login -->
                    <TextBlock HorizontalAlignment="Center">
                        <Run Text="Already have an account? "/>
                        <Hyperlink Click="BackToLogin_Click"
                                   Foreground="{StaticResource PrimaryBrush}">
                            Sign in
                        </Hyperlink>
                    </TextBlock>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
